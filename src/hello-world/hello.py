import pandas as pd
print("hello")
print("changes something in branch 001")
print("added changes to test merge")

Columns_df = ["Day", "Total"]
df_result_Totals = pd.DataFrame(columns=Columns_df)
print(df_result_Totals.info())